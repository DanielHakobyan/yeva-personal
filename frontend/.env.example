# Leave empty for local dev (Vite proxies /api → backend).
# For production, set to your API origin if the app is not same-origin, e.g. https://your-app.vercel.app
VITE_API_URL=
#
# Used for SEO canonical URLs / OG image URLs / sitemap hostname.
VITE_SITE_URL=
